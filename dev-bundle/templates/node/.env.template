# {{PROJECT_NAME}} Environment Variables
NODE_ENV=development
PORT=3000

# Database (if using MySQL from DevLauncher)
DB_HOST=localhost
DB_PORT=3306
DB_NAME={{PROJECT_NAME}}_db
DB_USER=devuser
DB_PASSWORD=devpass

# Redis (if using Redis from DevLauncher)
REDIS_HOST=localhost
REDIS_PORT=6379