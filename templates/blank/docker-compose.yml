services:
  workspace:
    image: alpine:latest
    container_name: {{SLUG}}-workspace
    command: tail -f /dev/null
    volumes:
      - ./:/workspace
    working_dir: /workspace
    networks:
      - {{SLUG}}-network

networks:
  {{SLUG}}-network:
    driver: bridge